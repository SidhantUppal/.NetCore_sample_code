//------------------------------------------------------------------------------
// <autogenerated>
//     
//     Changes to this file will be lost after each regeneration.
//     To extend the functionality of this class, please modify the partial class 'BudgetLine.cs'.
//
//     Template: EditableRoot.DataAccess.StoredProcedures.cst
//     
// </autogenerated>
//------------------------------------------------------------------------------
using System;
using System.Data;
using System.Data.SqlClient;
 using Dapper;

namespace CFMData
{
	public partial class BudgetLine
	{
    
		private BudgetLine DataPortal_Fetch(BudgetLineCriteria criteria)
		{
 
			bool cancel = false;
			OnFetching(criteria, ref cancel);
			if (cancel) return null;
			using (var connection = new SqlConnection(ADOHelper.ConnectionString))
			{
				connection.Open();
				using (var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Select]", connection))
				{
					command.CommandType = CommandType.StoredProcedure;
					command.Parameters.AddRange(ADOHelper.SqlParameters(criteria.StateBag));
                    command.Parameters.AddWithValue("@p_FortnightAmountHasValue", criteria.FortnightAmountHasValue);
                command.Parameters.AddWithValue("@p_UpdatedByHasValue", criteria.UpdatedByHasValue);
                command.Parameters.AddWithValue("@p_UpdatedOnHasValue", criteria.UpdatedOnHasValue);
					using(var reader = command.ExecuteReader())
					{
						var rowParser = reader.GetRowParser<BudgetLine>();                       
						if(reader.Read())
						{
							return GetBudgetLine(rowParser, reader);							
						}                            
						else
							throw new Exception(String.Format("The record was not found in 'dbo.BudgetLine' using the following criteria: {0}.", criteria));
					}
				}
			}
			OnFetched();
		}

       // [Transactional(TransactionalTypes.TransactionScope)]
		protected   void DataPortal_Insert()
		{
			bool cancel = false;
			OnInserting(ref cancel);
			if (cancel) return;
			using (var connection = new SqlConnection(ADOHelper.ConnectionString))
			{
				connection.Open();
				SqlTransaction trans = connection.BeginTransaction();
				try
				{
				
				using(var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Insert]", connection,trans))
				{
					command.CommandType = CommandType.StoredProcedure;
					
          command.Parameters.AddWithValue("@p_BudgetLineID", this.BudgetLineID);
                command.Parameters["@p_BudgetLineID"].Direction = ParameterDirection.Output;
                command.Parameters.AddWithValue("@p_BudgetID", this.BudgetID);
                command.Parameters.AddWithValue("@p_LineID", this.LineID);
                command.Parameters.AddWithValue("@p_OrderNo", this.OrderNo);
                command.Parameters.AddWithValue("@p_CategoryID", this.CategoryID);
                command.Parameters.AddWithValue("@p_FortnightAmount", ADOHelper.NullCheck(this.FortnightAmount));
                command.Parameters.AddWithValue("@p_RequiresReceipt", this.RequiresReceipt);
                command.Parameters.AddWithValue("@p_IsActive", this.IsActive);
                command.Parameters.AddWithValue("@p_CreatedBy", this.CreatedBy);
                command.Parameters.AddWithValue("@p_CreatedOn", this.CreatedOn);
                command.Parameters.AddWithValue("@p_UpdatedBy", ADOHelper.NullCheck(this.UpdatedBy));
                command.Parameters.AddWithValue("@p_UpdatedOn", ADOHelper.NullCheck(this.UpdatedOn));
					command.ExecuteNonQuery();                  
					_budgetLineIDProperty=(System.Int32)command.Parameters["@p_BudgetLineID"].Value;
                    
				}
                
				UpdateChildren(this, connection,trans);
				
				trans.Commit();
			}
			catch(Exception ex)
			{
				trans.Rollback();
				throw;
			}
			
		}
			

			OnInserted();

		}

       // [Transactional(TransactionalTypes.TransactionScope)]
		protected   void DataPortal_Update()
		{
			bool cancel = false;
			OnUpdating(ref cancel);
			if (cancel) return;
			using (var connection = new SqlConnection(ADOHelper.ConnectionString))
			{
				connection.Open();
				SqlTransaction trans = connection.BeginTransaction();
				try
				{
				using(var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Update]", connection,trans))
				{
					command.CommandType = CommandType.StoredProcedure;
					command.Parameters.AddWithValue("@p_BudgetLineID", this.BudgetLineID);
                command.Parameters.AddWithValue("@p_BudgetID", this.BudgetID);
                command.Parameters.AddWithValue("@p_LineID", this.LineID);
                command.Parameters.AddWithValue("@p_OrderNo", this.OrderNo);
                command.Parameters.AddWithValue("@p_CategoryID", this.CategoryID);
                command.Parameters.AddWithValue("@p_FortnightAmount", ADOHelper.NullCheck(this.FortnightAmount));
                command.Parameters.AddWithValue("@p_RequiresReceipt", this.RequiresReceipt);
                command.Parameters.AddWithValue("@p_IsActive", this.IsActive);
                command.Parameters.AddWithValue("@p_CreatedBy", this.CreatedBy);
                command.Parameters.AddWithValue("@p_CreatedOn", this.CreatedOn);
                command.Parameters.AddWithValue("@p_UpdatedBy", ADOHelper.NullCheck(this.UpdatedBy));
                command.Parameters.AddWithValue("@p_UpdatedOn", ADOHelper.NullCheck(this.UpdatedOn));
					//result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
					int result = command.ExecuteNonQuery();
					if (result == 0)
						throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.");

				}
				UpdateChildren(this, connection,trans);
				//FieldManager.UpdateChildren(this, connection);
				trans.Commit();
			}
			catch(Exception ex)
			{
				trans.Rollback();
				throw;
			}
			
		}

			OnUpdated();
		}
		protected   void UpdateChildren(BudgetLine parent,SqlConnection connection,SqlTransaction trans)
		{
		
		

		}

		protected   void DataPortal_DeleteSelf()
		{
			bool cancel = false;
			OnSelfDeleting(ref cancel);
			if (cancel) return;            
			DataPortal_Delete(new BudgetLineCriteria (BudgetLineID));        
			OnSelfDeleted();
		}
        
		//[Transactional(TransactionalTypes.TransactionScope)]
		protected void DataPortal_Delete(BudgetLineCriteria criteria)
		{
            bool cancel = false;
            OnDeleting(criteria, ref cancel);
            if (cancel) return;

            using (var connection = new SqlConnection(ADOHelper.ConnectionString))
            {
                connection.Open();
                using (var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Delete]", connection))
                {
                    command.CommandType = CommandType.StoredProcedure;
		
                    command.Parameters.AddRange(ADOHelper.SqlParameters(criteria.StateBag));

                    //result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                    int result = command.ExecuteNonQuery();
                    if (result == 0)
                        throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.");
                }
            }

            OnDeleted();
		}
		
		 #region Child_Insert
        /// <summary>
        /// Inserts data into the data base using the information in the current 
        ///    CSLA editable child business object of type <see cref="BudgetLine"/> 
        /// </summary>
        /// <returns></returns>
        public void Child_Insert(SqlConnection connection,SqlTransaction trans)
        {
            bool cancel = false;
            OnChildInserting(connection, ref cancel,trans);
            if (cancel) return;

            if(connection.State != ConnectionState.Open) connection.Open();
            using(var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Insert]", connection,trans))
            {
                command.CommandType = CommandType.StoredProcedure;
		
                command.Parameters.AddWithValue("@p_BudgetLineID", this.BudgetLineID);
                command.Parameters["@p_BudgetLineID"].Direction = ParameterDirection.Output;
                command.Parameters.AddWithValue("@p_BudgetID", this.BudgetID);
                command.Parameters.AddWithValue("@p_LineID", this.LineID);
                command.Parameters.AddWithValue("@p_OrderNo", this.OrderNo);
                command.Parameters.AddWithValue("@p_CategoryID", this.CategoryID);
                command.Parameters.AddWithValue("@p_FortnightAmount", ADOHelper.NullCheck(this.FortnightAmount));
                command.Parameters.AddWithValue("@p_RequiresReceipt", this.RequiresReceipt);
                command.Parameters.AddWithValue("@p_IsActive", this.IsActive);
                command.Parameters.AddWithValue("@p_CreatedBy", this.CreatedBy);
                command.Parameters.AddWithValue("@p_CreatedOn", this.CreatedOn);
                command.Parameters.AddWithValue("@p_UpdatedBy", ADOHelper.NullCheck(this.UpdatedBy));
                command.Parameters.AddWithValue("@p_UpdatedOn", ADOHelper.NullCheck(this.UpdatedOn));

                command.ExecuteNonQuery();
               
                // Update identity primary key value.
                _budgetLineIDProperty=(System.Int32)command.Parameters["@p_BudgetLineID"].Value;
            }

            UpdateChildren(this, connection,trans);

            OnChildInserted();
        }

        public void Child_Insert(ApplicationUser applicationUser, SqlConnection connection,SqlTransaction trans)
        {
            Child_Insert(applicationUser, null, null, connection,trans);
        }


        public void Child_Insert(Budget budget, SqlConnection connection,SqlTransaction trans)
        {
            Child_Insert(null, budget, null, connection,trans);
        }


        public void Child_Insert(BudgetLineCategory budgetLineCategory, SqlConnection connection,SqlTransaction trans)
        {
            Child_Insert(null, null, budgetLineCategory, connection,trans);
        }


        public void Child_Insert(ApplicationUser applicationUser, Budget budget, BudgetLineCategory budgetLineCategory, SqlConnection connection,SqlTransaction trans)
        {
            bool cancel = false;
            OnChildInserting(applicationUser, budget, budgetLineCategory, connection, ref cancel,trans);
            if (cancel) return;

            if(connection.State != ConnectionState.Open) connection.Open();
            using(var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Insert]", connection,trans))
            {
                command.CommandType = CommandType.StoredProcedure;
		
                command.Parameters.AddWithValue("@p_BudgetLineID", this.BudgetLineID);
                command.Parameters["@p_BudgetLineID"].Direction = ParameterDirection.Output;
                command.Parameters.AddWithValue("@p_BudgetID", budget != null ? budget.BudgetID : this.BudgetID);
                command.Parameters.AddWithValue("@p_LineID", this.LineID);
                command.Parameters.AddWithValue("@p_OrderNo", this.OrderNo);
                command.Parameters.AddWithValue("@p_CategoryID", budgetLineCategory != null ? budgetLineCategory.BudgetLineCategoryID : this.CategoryID);
                command.Parameters.AddWithValue("@p_FortnightAmount", ADOHelper.NullCheck(this.FortnightAmount));
                command.Parameters.AddWithValue("@p_RequiresReceipt", this.RequiresReceipt);
                command.Parameters.AddWithValue("@p_IsActive", this.IsActive);
                command.Parameters.AddWithValue("@p_CreatedBy", applicationUser != null ? applicationUser.ApplicationUserID : this.CreatedBy);
                command.Parameters.AddWithValue("@p_CreatedOn", this.CreatedOn);
                command.Parameters.AddWithValue("@p_UpdatedBy", ADOHelper.NullCheck(applicationUser != null ? applicationUser.ApplicationUserID : this.UpdatedBy));
                command.Parameters.AddWithValue("@p_UpdatedOn", ADOHelper.NullCheck(this.UpdatedOn));

                command.ExecuteNonQuery();
               
                // Update identity primary key value.
                _budgetLineIDProperty=(System.Int32)command.Parameters["@p_BudgetLineID"].Value;

            // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
            if(budget != null && budget.BudgetID != this.BudgetID)
                _budgetIDProperty= budget.BudgetID;

            // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
            if(budgetLineCategory != null && budgetLineCategory.BudgetLineCategoryID != this.CategoryID)
                _categoryIDProperty= budgetLineCategory.BudgetLineCategoryID;

            // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
            if(applicationUser != null && applicationUser.ApplicationUserID != this.CreatedBy)
                _createdByProperty= applicationUser.ApplicationUserID;

            // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
            if(applicationUser != null && applicationUser.ApplicationUserID != this.UpdatedBy)
                _updatedByProperty= applicationUser.ApplicationUserID;
            }
            
            // A child relationship exists on this Business Object but its type is not a child type (E.G. EditableChild). 
            // TODO: Please override OnChildInserted() and insert this child manually.
            // UpdateChildren(this, connection);

            OnChildInserted();
        }

        #endregion

        #region Child_Update

        /// <summary>
        /// Updates the corresponding record in the data base with the information in the current 
        ///    CSLA editable child business object of type <see cref="BudgetLine"/> 
        /// </summary>
        /// <returns></returns>
        public void Child_Update(SqlConnection connection,SqlTransaction trans)
        {
            bool cancel = false;
            OnChildUpdating(connection, ref cancel,trans);
            if (cancel) return;

            if(connection.State != ConnectionState.Open) connection.Open();
            using(var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Update]", connection,trans))
            {
                command.CommandType = CommandType.StoredProcedure;
		
                command.Parameters.AddWithValue("@p_BudgetLineID", this.BudgetLineID);
                command.Parameters.AddWithValue("@p_BudgetID", this.BudgetID);
                command.Parameters.AddWithValue("@p_LineID", this.LineID);
                command.Parameters.AddWithValue("@p_OrderNo", this.OrderNo);
                command.Parameters.AddWithValue("@p_CategoryID", this.CategoryID);
                command.Parameters.AddWithValue("@p_FortnightAmount", ADOHelper.NullCheck(this.FortnightAmount));
                command.Parameters.AddWithValue("@p_RequiresReceipt", this.RequiresReceipt);
                command.Parameters.AddWithValue("@p_IsActive", this.IsActive);
                command.Parameters.AddWithValue("@p_CreatedBy", this.CreatedBy);
                command.Parameters.AddWithValue("@p_CreatedOn", this.CreatedOn);
                command.Parameters.AddWithValue("@p_UpdatedBy", ADOHelper.NullCheck(this.UpdatedBy));
                command.Parameters.AddWithValue("@p_UpdatedOn", ADOHelper.NullCheck(this.UpdatedOn));

                //result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                int result = command.ExecuteNonQuery();
                if (result == 0)
                    throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.");
            }

            UpdateChildren(this, connection,trans);

            OnChildUpdated();
        }

        public void Child_Update(ApplicationUser applicationUser, SqlConnection connection,SqlTransaction trans)
        {
            Child_Update(applicationUser, null, null, connection,trans);
        }


        public void Child_Update(Budget budget, SqlConnection connection,SqlTransaction trans)
        {
            Child_Update(null, budget, null, connection,trans);
        }


        public void Child_Update(BudgetLineCategory budgetLineCategory, SqlConnection connection,SqlTransaction trans)
        {
            Child_Update(null, null, budgetLineCategory, connection,trans);
        }

 
        public void Child_Update(ApplicationUser applicationUser, Budget budget, BudgetLineCategory budgetLineCategory, SqlConnection connection,SqlTransaction trans)
        {
            bool cancel = false;
            OnChildUpdating(applicationUser, budget, budgetLineCategory, connection, ref cancel,trans);
            if (cancel) return;

            if(connection.State != ConnectionState.Open) connection.Open();
            using(var command = new SqlCommand("[dbo].[spCFM_BudgetLine_Update]", connection,trans))
            {
                command.CommandType = CommandType.StoredProcedure;
		
                command.Parameters.AddWithValue("@p_BudgetLineID", this.BudgetLineID);
                command.Parameters.AddWithValue("@p_BudgetID", budget != null ? budget.BudgetID : this.BudgetID);
                command.Parameters.AddWithValue("@p_LineID", this.LineID);
                command.Parameters.AddWithValue("@p_OrderNo", this.OrderNo);
                command.Parameters.AddWithValue("@p_CategoryID", budgetLineCategory != null ? budgetLineCategory.BudgetLineCategoryID : this.CategoryID);
                command.Parameters.AddWithValue("@p_FortnightAmount", ADOHelper.NullCheck(this.FortnightAmount));
                command.Parameters.AddWithValue("@p_RequiresReceipt", this.RequiresReceipt);
                command.Parameters.AddWithValue("@p_IsActive", this.IsActive);
                command.Parameters.AddWithValue("@p_CreatedBy", applicationUser != null ? applicationUser.ApplicationUserID : this.CreatedBy);
                command.Parameters.AddWithValue("@p_CreatedOn", this.CreatedOn);
                command.Parameters.AddWithValue("@p_UpdatedBy", ADOHelper.NullCheck(applicationUser != null ? applicationUser.ApplicationUserID : this.UpdatedBy));
                command.Parameters.AddWithValue("@p_UpdatedOn", ADOHelper.NullCheck(this.UpdatedOn));

                //result: The number of rows changed, inserted, or deleted. -1 for select statements; 0 if no rows were affected, or the statement failed. 
                int result = command.ExecuteNonQuery();
                if (result == 0)
                    throw new DBConcurrencyException("The entity is out of date on the client. Please update the entity and try again. This could also be thrown if the sql statement failed to execute.");

                // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
                if(budget != null && budget.BudgetID != this.BudgetID)
                    _budgetIDProperty= budget.BudgetID;

                // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
                if(budgetLineCategory != null && budgetLineCategory.BudgetLineCategoryID != this.CategoryID)
                    _categoryIDProperty= budgetLineCategory.BudgetLineCategoryID;

                // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
                if(applicationUser != null && applicationUser.ApplicationUserID != this.CreatedBy)
                    _createdByProperty= applicationUser.ApplicationUserID;

                // Update foreign keys values. This code will update the values passed in from the parent only if no errors occurred after executing the query.
                if(applicationUser != null && applicationUser.ApplicationUserID != this.UpdatedBy)
                    _updatedByProperty= applicationUser.ApplicationUserID;
            }
            
            // A child relationship exists on this Business Object but its type is not a child type (E.G. EditableChild). 
            // TODO: Please override OnChildUpdated() and update this child manually.
            // UpdateChildren(this, connection);

            OnChildUpdated();
        }
        #endregion

        /// <summary>
        /// Deletes the corresponding record in the data base with the information in the current 
        ///    CSLA editable child business object of type <see cref="BudgetLine"/> 
        /// </summary>
        /// <returns></returns>
        private void Child_DeleteSelf(SqlConnection connection)
        {
            bool cancel = false;
            OnChildSelfDeleting(connection, ref cancel);
            if (cancel) return;
            
            //DataPortal_Delete(new BudgetLineCriteria (BudgetLineID), connection);
        
            OnChildSelfDeleted();
        }

        #region Map
		public void InitDTO()
		{
			  BudgetLineDTO dt=new BudgetLineDTO();
			dt.BudgetLineID =this.BudgetLineID ;
			dt.BudgetID =this.BudgetID ;
			dt.LineID =this.LineID ;
			dt.OrderNo =this.OrderNo ;
			dt.CategoryID =this.CategoryID ;
			dt.FortnightAmount =this.FortnightAmount ;
			dt.RequiresReceipt =this.RequiresReceipt ;
			dt.IsActive =this.IsActive ;
			dt.CreatedBy =this.CreatedBy ;
			dt.CreatedOn =this.CreatedOn ;
			dt.UpdatedBy =this.UpdatedBy ;
			dt.UpdatedOn =this.UpdatedOn ;
   //LoadProperty(_currentDto, dt);
  this.CurrentDTO = dt;

		}
		/*
        private void Map(SafeDataReader reader)
        {
            bool cancel = false;
            OnMapping(reader, ref cancel);
            if (cancel) return;

            using(BypassPropertyChecks)
            {
                LoadProperty(_budgetLineIDProperty, reader["BudgetLineID"]);
                LoadProperty(_budgetIDProperty, reader["BudgetID"]);
                LoadProperty(_lineIDProperty, reader["LineID"]);
                LoadProperty(_orderNoProperty, reader["OrderNo"]);
                LoadProperty(_categoryIDProperty, reader["CategoryID"]);
                LoadProperty(_fortnightAmountProperty, reader["FortnightAmount"]);
                LoadProperty(_requiresReceiptProperty, reader["RequiresReceipt"]);
                LoadProperty(_isActiveProperty, reader["IsActive"]);
                LoadProperty(_createdByProperty, reader["CreatedBy"]);
                LoadProperty(_createdOnProperty, reader["CreatedOn"]);
                LoadProperty(_updatedByProperty, reader["UpdatedBy"]);
                LoadProperty(_updatedOnProperty, reader["UpdatedOn"]);
            }	
			InitDTO();
            OnMapped();
        }
        
        private void Child_Fetch(SafeDataReader reader)
        {
            Map(reader);
        }
		*/

        #endregion
	}
}
 